version: '3.8'

services:
  umnorder:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # Persist database
    environment:
      - DATABASE_URL=sqlite:///./data/umnorder.db
    restart: unless-stopped
